# Python-generated files
__pycache__/
*.py[oc]
build/
dist/
wheels/
*.egg-info

# Compiled C++ extension (issue 009) -- built by `uv sync` via setup.py, never checked in.
*.so

# Virtual environments
.venv
.env
.claude/
analysis/
issues/
clean_scripts/
converted_scripts/
docs/
kaggle_notebooks/
kaggle_discussions/
wiki/
submissions/
CLAUDE.md
COMPETITION_DATA.md
CONTEXT.md
PLAN.md
orbit_wars_catalogue.csv
pipeline.py

*.csv
*.json
# ...except the vendored engine spec (issue 002): model/constants.py loads it at import time,
# so a git clone missing it silently breaks kaggriculture.model and everything built on it.
!vendor/kaggriculture.json
